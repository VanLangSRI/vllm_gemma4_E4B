# The stack every number in the README is measured on. vllm==0.27.1 pins
# torch 2.13.0 (cu130), triton 3.7.1 and flashinfer-python itself; the rest is
# pinned to what the reference venv resolved to.
vllm==0.27.1
transformers==5.15.1
tokenizers==0.22.2
compressed-tensors==0.17.0
huggingface_hub==1.28.0
hf_transfer==0.1.9
ninja==1.13.4
# bench/run_benchmarks.sh calls `vllm bench serve`, whose dataset loader imports
# pandas through a PlaceholderModule -- so without this every cohort dies on
# "Please install vllm[bench] for bench support" and the fix is not obvious
# from the message (same gotcha as the reference repo's #22). Installing the
# vllm[bench] extra instead would reinstall vLLM and revert the patch stack.
pandas==3.0.5
av==18.1.0
pyarrow
